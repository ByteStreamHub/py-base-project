# {{ project_title | default('project', false) }}

{{ project_description | default('project description', false) }}

## Overview

### TBD

## Features

### TBD

## Project Configuration

- **Python Version**: {{ python_version | default(3.10, false) }} (compatible with {{ requires_python_version | default(3.10, false) }}+)
- **Package Management**: [UV](https://docs.astral.sh/uv/)
- **Project Name**: {{ project_name | default('project', false) }}

## Development Tools

### Linters & Formatters

- [Ruff](https://docs.astral.sh/ruff/) - Fast Python linter and formatter
- [Pyright](https://github.com/microsoft/pyright) - Static type checker for Python

### Testing

- [PyTest](https://docs.pytest.org/en/stable/) - Testing framework with coverage reporting

## Getting Started

Description TBD

### Installation

```bash
# Clone the repository
git clone {{ project_git_repo }}
cd your-project

# Install dependencies and configure development environment
make local-dev-config
```

### Using the Makefile

TBD

#### Development Setup

```bash
# Configure development environment (installs Python, dependencies, and sets up pre-commit hooks)
make local-config
```

#### Code Quality

```bash
# Format code using Ruff
make format

# Run linters (Ruff and Pyright)
make lint

# Run tests with coverage
make test

# Run format, lint, and test in sequence
make check

# Clean project artifacts
make clean

# Build the project and create version and latest files
make build
```

#### Project Management

```bash
# Generate changelog from git history
make changelog-generate
```

#### Release Management

```bash
# Create a release with a patch version bump (e.g., 1.0.0 → 1.0.1)
make release-patch

# Create a release with a minor version bump (e.g., 1.0.0 → 1.1.0)
make release-minor

# Create a release with a major version bump (e.g., 1.0.0 → 2.0.0)
make release-major
```

For a complete list of available commands, run:

```bash
make help
```

## Development Workflow

### TBD

## Release Process

### How to Release

## To create a new release:

### TBD
